schema_version: 1

description: "The WildFly runtime image, JDK 25"
version: &imgVersion "2.2.1-jdk25"
from: "registry.access.redhat.com/ubi10/ubi-minimal"
labels:
  - name: "org.jboss.product.version"
    value: *imgVersion
envs:
  - name: IMAGE_VERSION
    value: *imgVersion
modules:
      repositories:
          - name: openjdk
            git:
              url: https://github.com/rh-openjdk/redhat-openjdk-containers
              ref: ubi10-wildfly-container-40.0
      install:
          - name: jboss.container.openjdk.jdk
            version: "25"
