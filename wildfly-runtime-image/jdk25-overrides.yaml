schema_version: 1

description: "The WildFly runtime image, JDK 25"
version: &imgVersion "2.2.1-jdk25-snapshot"
labels:
  - name: "org.jboss.product.version"
    value: *imgVersion
envs:
  - name: IMAGE_VERSION
    value: *imgVersion
modules:
      install:
          - name: jboss.container.openjdk.jdk
            version: "25"
